# SECRETS ONLY - Copy this file to .env and fill in your secrets
# This file should be added to .gitignore!
# Use 'export' because we are using direnv's 'source_env'

# SSH Key
export ADMIN_SSH_PUBLIC_KEY="ssh-rsa ..." # Your SSH public key

# Age encryption keys for sops-nix
export AGE_PUBLIC_KEY="age1..." # Your age public key
export AGE_PRIVATE_KEY="AGE-SECRET-KEY-..." # Your age private key

# Hetzner Cloud API token
export HETZNER_API_TOKEN="your-hetzner-api-token"

# GitHub personal access token
export GITHUB_TOKEN="ghp_..." # Handle securely if repo public

# Tailscale auth key
export TAILSCALE_AUTH_KEY="tskey-auth-..." # Needed for sops encrypt step

# K3S token - generate with: openssl rand -hex 32
export K3S_TOKEN="generated-k3s-token-value"

# MinIO credentials
export MINIO_SECRET_KEY="your-minio-secret-key"

# Attic configuration
export ATTIC_CACHE_KEY="attic-1:..." # Your Attic cache key
export ATTIC_TOKEN_SIGNING_SECRET="a-very-strong-random-secret-for-attic-tokens" # For Infisical->Attic secret sync

# Infisical service token
export INFISICAL_SERVICE_TOKEN="st.xyz..." # Token for Infisical Operator bootstrap secret

# SigNoz (Observability)
# export SIGNOZ_INGESTION_KEY="xyz..." # Optional: Uncomment and set if your SigNoz backend requires authentication